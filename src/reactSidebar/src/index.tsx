import * as React from 'react';
import * as ReactDOM from 'react-dom';
import {SubscribeForm} from './sidebarComponent';


export function start(element) {

    ReactDOM.render(
        <SubscribeForm></SubscribeForm>,
        element
    );
}
